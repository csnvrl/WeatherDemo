import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  cityText: {
    fontSize: 22,
    textAlign: 'center',
  },
  infoText: {
    fontSize: 18,
    textAlign: 'center',
  },
  lastUpdate: {
    fontSize: 15,
    textAlign: 'center',
  },
});
